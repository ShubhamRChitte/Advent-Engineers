const mongoose = require('mongoose');
const TransformerModel = require('./schema/TransformerSchema'); // Adjust path if needed
const OrderModel = require('./schema/OrderSchema');

async function check() {
    await mongoose.connect('mongodb://localhost:27017/advent-shubham'); // Adjust DB name
    const t = await TransformerModel.findOne({ uniqueId: 'TRANS-ID' }); // Replace with actual test ID if known
    console.log(JSON.stringify(t.testHistory, null, 2));
    process.exit();
}
// check();
